#!/usr/bin/env bash
# Adapter from grover's pre-remove hook contract to retirable's
# bin/teardown-worktree, which takes no args and runs from the worktree.
set -euo pipefail
exec "$GROVER_WORKTREE_PATH/bin/teardown-worktree"
