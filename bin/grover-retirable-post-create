#!/usr/bin/env bash
# Adapter from grover's post-create hook contract to retirable's
# bin/setup-worktree, which takes the primary checkout as a positional arg.
set -euo pipefail
exec "$GROVER_WORKTREE_PATH/bin/setup-worktree" "$GROVER_REPO_PATH"
